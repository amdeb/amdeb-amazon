<?xml version="1.0" encoding="UTF-8"?>
<openerp>
    <data>
        <record id="view_amdeb_amazon_configuration" model="ir.ui.view">
            <field name="name">amdeb amazon settings</field>
            <field name="model">amdeb.amazon.config.settings</field>
            <field name="arch" type="xml">

                <form string="Amazon Integration Configuration"
                      class="oe_form_configuration">

                    <header>
                        <button string="Apply"
                                type="object"
                                name="execute"
                                class="oe_highlight"/>
                        or
                        <button string="Cancel"
                                type="object"
                                name="cancel"
                                class="oe_link"/>
                    </header>

                    <group string="Account Settings">
                        <field name="account_id"/>
                        <field name="access_key"/>
                        <field name="secrete_key"/>

                    </group>

                    <group string="Integration Settings">
                        <field name="integration_interval"/>
                        <field name="automatic_flag"/>
                    </group>

                </form>
            </field>
        </record>

        <record id="action_amdeb_amazon_configuration"
                model="ir.actions.act_window">
            <field name="name">
                Amdeb Amazon Configuration
            </field>
            <field name="res_model">
                amdeb.amazon.config.settings
            </field>
            <field name="view_mode">
                form
            </field>
            <field name="target">
                inline
            </field>
        </record>

        <menuitem id="menu_amdeb_amazon_config"
                  name="Amazon Settings"
                  parent="base.menu_config"
                  action="action_amdeb_amazon_configuration"/>
    </data>
</openerp>